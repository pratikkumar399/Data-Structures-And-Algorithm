#include <bits/stdc++.h>
using namespace std;

void preSum(int n)
{
}

void superSum()
{
}

int main()
{
    int t;
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cin >> t;
    while (t--)
    {
        int a;
        cin >> a;
        vector<int> arr(a);
        for (int i = 0; i < a; i++)
        {
            cin >> arr[i];
        }

        preSum(n);
        superSum(n);
    }

    return 0;
}